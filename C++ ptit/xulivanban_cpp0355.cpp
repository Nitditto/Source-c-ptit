#include<bits/stdc++.h>
using namespace std;

void Viethoa(string s){
	for(int i=0;i<s.length();i++){
		s[i]=
	}
}
int main(){
	vector<string>s;
	string w;
	while(cin>>w){
		s.push_back(w);
	}
	for(int i=0;i<s.size();i++){
		if(s[i]==''||s[i])
	}
	while(ss>>token){
		v.push_back(token);
	}
	for(int i=0;i<v.size();i++){
		cout<<v[i];
	}
}
